{% extends 'base.html.twig' %}
{% import 'macro/global.html.twig' as global %}
{% import 'macro/list-posts.html.twig' as listPosts %}

{% block title %}{{ parent() ~  " | list of posts from the category : " ~ currentCategory.title }}{% endblock %}

{% block body %}
    {{ global.pageTitle("list of posts from the category : " ~ currentCategory.title) }}

    <!-- list of posts -->
    {{ listPosts.list(posts) }}
    {{ listPosts.pagination(posts) }}
{% endblock %}
