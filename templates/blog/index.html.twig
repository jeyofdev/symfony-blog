{% extends 'base.html.twig' %}
{% import 'macro/global.html.twig' as global %}
{% import 'blog/parts/macro-list-posts.html.twig' as listPosts %}

{% block title %}{{ parent() ~  " | List of posts" }}{% endblock %}

{% block body %}
    {{ global.pageTitle("List of blog posts") }}

    <!-- flash message -->
    {% for message in app.flashes('success') %}
        <p class="alert alert-success my-5">
            {{ message }}
        </p>
    {% endfor %}

    <!-- list of posts -->
    {{ include ("blog/parts/_list.html.twig") }}
    {{ global.pagination(posts) }}
{% endblock %}
