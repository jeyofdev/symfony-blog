{% extends 'base.html.twig' %}
{% import 'macro/global.html.twig' as global %}
{% import 'macro/list-posts.html.twig' as listPosts %}

{% block title %}{{ parent() ~  " | List of posts" }}{% endblock %}

{% block body %}
    {{ global.pageTitle("List of blog posts") }}

    <!-- list of posts -->
    {{ listPosts.list(posts) }}
    {{ listPosts.pagination(posts) }}
{% endblock %}
